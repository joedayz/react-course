import React from 'react'
import ReactDOM from 'react-dom/client'

import {FirstApp} from "./FirstApp.jsx";

import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <FirstApp subTitle="adsabads"/>
    </React.StrictMode>
);
